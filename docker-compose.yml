services:
  web:
    image: node:22-alpine
    container_name: anadinhohub_web
    working_dir: /app
    ports:
      - "5173:5173"
    volumes:
      - .:/app
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
