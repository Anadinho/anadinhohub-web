services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: anadinhohub_web
    working_dir: /app
    ports:
      - "${WEB_PORT:-5173}:5173"
    volumes:
      - .:/app
    command: npm run dev -- --host 0.0.0.0
