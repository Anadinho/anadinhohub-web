services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL}
    container_name: anadinhohub_web
    restart: unless-stopped
    ports:
      - "${WEB_PORT:-8080}:80"
